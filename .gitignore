# ============================================================
# Lo que NUNCA se sube al repositorio
# ============================================================

# Las piezas que instala npm. Se rehacen con "npm install".
node_modules/

# TUS DATOS. La carpeta completa: base de datos, respaldos y el logo
# que subiste desde Personalizar.
#
# Esta carpeta es el negocio y vive solo en la PC de la fábrica. Por eso
# nunca aparece en Git y nunca se pierde al actualizar el sistema.
/datos/

# Configuración de la máquina y basura del sistema operativo
.env
.DS_Store
Thumbs.db
*.log
